package com.challenges.googleplaystore;

import java.io.*;
import java.util.*; 

import org.apache.poi.ss.usermodel.*;
import org.apache.poi.xssf.usermodel.*;

/*
* 2).write a program to list all available categories (no duplicate names)
* Run the command from the root directory of this project to validate if this Question is answered correctly
* './gradlew test -Dtest.single='*googleplaystore/Question2''
*/

public class Question2_Solution{

    /**
    *Note:While returning the result as a map, in key:value pair always use key as "appsCategory"
    * A sample return Map look like this:
    * ["appsCategory":["GO Notifier",..]]
    */
    
    public static Map solve(File file){
      //YOUR CODE GOES HERE
      return null;
    }
}
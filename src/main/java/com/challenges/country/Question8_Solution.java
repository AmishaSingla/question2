package com.challenges.country;

import java.io.*;
import java.util.*;

import org.apache.poi.ss.usermodel.*;
import org.apache.poi.xssf.usermodel.*;

/*
* 8).Identify the country with the highest death rate (If Deathrate attribute is empty,
* it means that Death rate for that Country is not known)
* Run the command from the root directory of this project to validate if this Question is
* answered correctly
* './gradlew test -Dtest.single='*country/Question8''
*/

public class Question8_Solution{

    /**
    * Note:While returning the result as a map, in key:value pair always use key as "countryName"
    * A sample return Map look like this:
    * {"countryName":["Italy"]}
    */

    public static Map solve(File file){
      //YOUR CODE GOES HERE
      return null;
    }

}